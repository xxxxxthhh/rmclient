# rmclient v0 spec（草案，2026-08-28）

> 形态倾向已定（本地 Web），里程碑切分待用户确认后再细化验收标准。

## 形态

本地 Web 应用：单 Python 进程（FastAPI），浏览器开 `http://localhost:<port>`。
理由：两个核心场景（拖书上传、看笔记渲染）都是视觉交互；Python 全量复用
rmscene/httpx 与已验证契约；自用免打包。API 客户端层独立成模块，顺带暴露
CLI 命令（`rmclient push book.epub` 一类）。

## 里程碑

**M1 — 推书（核心价值，先闭环）**
- 页面拖入 epub/pdf → 上传到指定目录；CLI 同能力。
- 客户端侧校验扩展名与内容一致性（服务端不校验，见纪律 5）。
- 先补测：同名/重复上传语义（覆盖？双份？409？）——决定「更新一本书」怎么做。
- ✅ 验收：拖一本真实 epub，设备同步后能打开；重复推同一本书行为符合预期并有提示。

**M2 — 树浏览与管理**
- 文档树展示（目录/文档、类型、大小、时间），`Mailbox/` 显示但锁定只读。
- 移动（保留原名）、重命名、新建目录；删除带二次确认 + 复活复查提示。
- ✅ 验收：整理一次真实文件（本 spike 期间手工做过的操作全部界面化）。

**M3 — 看笔记**
- 选中 notebook → 导出 rmdoc → rmscene 解析 → 渲染 SVG/PNG 预览；可导出 PDF。
- ✅ 验收：打开 `Mailbox/` 里任一真实笔记（只读导出），页面渲染人眼可辨认。

## 明确不做（v0）

- 不动服务端、不做多用户/多设备、不做设备端组件。
- 不做自动监听文件夹同步（先手动拖，需求真实存在再加）。
- 不做 epub 排版参数预设（要走 .rmdoc 路线，开销大收益小）。

## 已知开放问题（继承自 spike REPORT §8）

- ~~重复上传语义（M1 先测）~~ ✅ 已测（REPORT §10）：无条件新建、不覆盖不去重不 409；客户端做重名检测 + `--force` 显式确认。
- 大文件是否撞 Cloudflare 100MB / 隧道超时（真实大书实测即可）。
- 封面/书籍元数据在设备库的呈现。
